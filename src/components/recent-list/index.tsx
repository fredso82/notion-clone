import { View } from "react-native";
type Props = {
    data: {
        id: string
        title: string
        cover?: string
    }[]
}

export function RecentList({ data }: Props) {
    return (
        <View></View>
    )
}